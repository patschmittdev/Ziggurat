{
  "name": "ziggurat-site",
  "version": "0.1.0",
  "description": "Documentation site for Ziggurat, a human-gated memory firewall.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync": "astro sync",
    "typecheck": "astro check",
    "validate": "node scripts/validate-build.mjs",
    "check": "npm run typecheck && npm run build && npm run validate"
  },
  "engines": { "node": ">=22.12.0" },
  "dependencies": {
    "@astrojs/starlight": "^0.41.10",
    "astro": "^7.2.9"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.0"
  }
}
